# Badge

```typescript
type BadgeProps = ViewProps & {
  label?: string;
  type?: 'default' | 'dot' | 'label';
};
```

- `default` — red circle with number
- `dot` — small red dot
- `label` — text label badge
