# Pagination

```typescript
type PaginationProps = {
  type?: 'default' | 'black_white' | 'number' | 'scroll';
  activeIndex: number;
  dataLength: number;
};
```

Page indicator dots/numbers for carousels.
