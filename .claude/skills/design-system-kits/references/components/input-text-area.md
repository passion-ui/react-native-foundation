# InputTextArea

Multi-line text input. Same props as [Input](input.md) but renders as a multi-line text area.

## Usage

> Wrap in a `Card` for proper background. See [Input](input.md).

```tsx
<Card>
  <InputTextArea floatingValue="Description" placeholder="Enter description..." numberOfLines={4} />
</Card>
```
